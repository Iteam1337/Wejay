describe('User', function() {

  beforeEach(module('wejay'));

descibe('facebookLogin')
  xit('should have tests', inject(function(User) {

	  //expect(User.doSomething()).to.equal('something');

  }));

});